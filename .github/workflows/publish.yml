name: Publish image

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.4.2"

      - name: Install
        run: bun install --frozen-lockfile

      - uses: docker/setup-buildx-action@v3

      - name: Build sitespeed fonts image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.sitespeed
          push: false
          load: true
          tags: ghcr.io/kriakiku/potato-sitespeed.io:40.0.0-plus1
          cache-from: type=gha,scope=sitespeed-fonts
          cache-to: type=gha,mode=max,scope=sitespeed-fonts

      - name: E2E sitespeed CLI (journey + --multi)
        run: bun run e2e:sitespeed-cli

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=sha-

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
